import { DependencySelectPipe } from './dependency-select.pipe';

describe('DependencySelectPipe', () => {
  it('create an instance', () => {
    const pipe = new DependencySelectPipe();
    expect(pipe).toBeTruthy();
  });
});
